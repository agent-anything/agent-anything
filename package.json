{
  "name": "agent-anything",
  "version": "0.0.0",
  "description": "A TypeScript platform for building tool-using AI agent products with structured tasks, permissions, evidence, and runtime output.",
  "scripts": {
    "api:check": "pnpm build && node scripts/check-built-public-apis.mjs",
    "architecture:test": "node --test scripts/architecture/*.test.mjs",
    "boundaries": "node scripts/check-boundaries.mjs",
    "build": "pnpm -r run build",
    "conformance:test": "pnpm --filter @agent-anything/host exec vitest run src/RunnerHostConformance.test.ts && pnpm --filter @agent-anything/helarc exec vitest run src/work-context/HelarcWorkContextCommit.test.ts && pnpm --filter @agent-anything/helarc-desktop exec vitest run src/main/run/HelarcHostRunComposition.test.ts src/main/HelarcMainController.test.ts src/main/HelarcDesktopProjection.test.ts src/main/ipc.test.ts src/renderer/App.test.tsx",
    "test": "pnpm run boundaries && pnpm run architecture:test && pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "keywords": [],
  "author": "vincent",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "private": true
}
